#!/usr/bin/env bash
# Entry point for the engram CLI
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Prefer the venv python if it exists
if [ -f "$DIR/venv/bin/python3" ]; then
    PYTHON="$DIR/venv/bin/python3"
else
    PYTHON="python3"
fi

exec "$PYTHON" "$DIR/engram_cli.py" "$@"
